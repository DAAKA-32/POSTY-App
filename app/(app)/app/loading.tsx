/**
 * App route loading state.
 * Minimal invisible placeholder to prevent loader flash during navigation.
 */
export default function AppLoading() {
  return (
    <div className="min-h-screen bg-transparent" />
  );
}
